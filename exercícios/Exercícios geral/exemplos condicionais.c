#include <stdio.h>
#include <stdlib.h>
#include <locale.h>;

int n;

main() {
    setlocale(LC_ALL, "Portuguese");

    n = 5

    if(n < 10){

    }
    else{
        if(n >1 ){

        }
    }

    switch(n){
        case 1:

        case 2:

        default:
    }
}
